# Talos ServiceAccount — grants the controller access to Talos gRPC API.
# The Talos CRD controller watches this resource, validates the namespace
# is in kubernetesTalosAPIAccess.allowedKubernetesNamespaces, generates
# a TLS client certificate, and creates a K8s Secret with the talosconfig.
#
# Prerequisites:
#   All managed nodes must have this in their machineconfig:
#     machine:
#       features:
#         kubernetesTalosAPIAccess:
#           enabled: true
#           allowedRoles: [os:admin]
#           allowedKubernetesNamespaces: [tfc-system]
apiVersion: talos.dev/v1alpha1
kind: ServiceAccount
metadata:
  name: tfc-talos-api
  namespace: tfc-system
spec:
  roles:
    - os:admin
